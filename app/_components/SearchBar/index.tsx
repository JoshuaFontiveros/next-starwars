import React, { FunctionComponent, ReactElement } from 'react';

const SearchBar: FunctionComponent = (): ReactElement => {
  return <div className='border'>Search bar</div>;
};

export default SearchBar;
